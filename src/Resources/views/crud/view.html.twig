{% extends "crud/layout.html.twig" %}

{% block content %}
    <table class="table">
        <thead>
        <tr>
            <th>#</th>
            <th>Username</th>
            <th>Date of birth</th>
            <th>Photo</th>
        </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.username }}</td>
                <td>{{ user.dob.format('Y-m-d') }}</td>
                <td>{{ user.photo }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}